# TODO

- [ ] Remover a seção "Sugestão de Arquitetura & Stack Técnica" (bloco de detecção de palavras-chave, ex: "No-Code / Plataformas Externas", "whatsapp", "bot") em [src/components/AiDiagnosticView.tsx](src/components/AiDiagnosticView.tsx)
- [ ] Exibir sempre o nome/descrição completa da etapa (não só o prefixo E0-E6) em todos os locais que mostram `GovStage`, usando `STAGE_NAMES`/`STAGE_DESCRIPTIONS` (src/data/estimationCatalog.ts). Gaps encontrados em [src/components/EstimationScheduleView.tsx](src/components/EstimationScheduleView.tsx):
  - Linhas 156-157: `action`/`description` do log de auditoria montam a string com os códigos crus (`${currentGovStage} → ${newStage}`), exibida verbatim na linha 923 ("Avanço de etapa da esteira: E1 → E2"); trocar para nomes via `STAGE_NAMES`.
  - Linha 709: badge de custo de módulo mostra só `({mod.stageId})` (ex: "+4h (E5)"), sem nome — comparar com o bloco de descontos (linha 779) que já resolve via `STAGE_NAMES[disc.stageId]`.
  - Linhas 976 e 981: modal de confirmação de avanço de etapa mostra `{pendingStageModal.targetStage}` cru ("Avançando para a etapa E6"), sem passar por `STAGE_NAMES`.
- [ ] Restringir perfil "padrão" (funcionário da área responsável pelo projeto, não técnico) para acesso **somente leitura**: ele deve conseguir consultar a ficha do projeto, artefatos e links, mas não cadastrar nem modificar nada — sem acesso ao módulo de parametrização (Configurações) nem ao módulo de criação de projeto/solução. Ajustar em [src/utils/permissions.ts](src/utils/permissions.ts): hoje o perfil `padrao` ainda tem `toggle_action_item`, `add_activity_log` e `add_notes` liberados (linhas 42-46) — remover essas permissões, deixando só `view_project`. Módulos de criação (`create_project`/`create_solution` em [Sidebar.tsx](src/components/Sidebar.tsx), [NewProjectPage.tsx](src/components/NewProjectPage.tsx), [ProjectPortfolioDashboard.tsx](src/components/ProjectPortfolioDashboard.tsx)) já são condicionados por `can(...)`, então devem ficar ocultos automaticamente após o ajuste — revisar também se a rota/aba de Configurações precisa de um bloqueio explícito além do botão do Sidebar.
- [ ] Renomear "Diário de Bordo" para "Acompanhamento" em todo o projeto (nomenclatura corporativa e sucinta). Ocorrências encontradas:
  - [src/components/ArtifactsDiaryView.tsx](src/components/ArtifactsDiaryView.tsx): nome do componente/interface (`ArtifactsDiaryView`/`ArtifactsDiaryViewProps`, linhas 38 e 67), textos de UI nas linhas 420, 467, 504, 730 (comentário), 781, 1109 (comentário) e 1114.
  - [src/components/ProjectWorkspaceHeader.tsx:58](src/components/ProjectWorkspaceHeader.tsx#L58): label da aba "Artefatos & Diário de Bordo".
  - [src/components/GlpiAssetView.tsx:376](src/components/GlpiAssetView.tsx#L376): texto "Artefatos & Diário de Bordo".
  - [src/services/supabase.ts:385](src/services/supabase.ts#L385): comentário "Atualizar diário de bordo / atas".
  - [src/App.tsx](src/App.tsx): import e uso de `ArtifactsDiaryView` (linhas 34 e 454) — considerar renomear o componente/arquivo junto (ex: `ProjectFollowUpView`/`AcompanhamentoView`) para manter consistência entre nome de código e nome exibido.

# TODO

- [x] Remover a seção "Sugestão de Arquitetura & Stack Técnica" (bloco de detecção de palavras-chave, ex: "No-Code / Plataformas Externas", "whatsapp", "bot") em [src/components/AiDiagnosticView.tsx](src/components/AiDiagnosticView.tsx)
- [x] Exibir sempre o nome/descrição completa da etapa (não só o prefixo E0-E6) em todos os locais que mostram `GovStage`, usando `STAGE_NAMES`/`STAGE_DESCRIPTIONS` (src/data/estimationCatalog.ts). Corrigido em [src/components/EstimationScheduleView.tsx](src/components/EstimationScheduleView.tsx) (log de auditoria, badge de módulo e modal de confirmação de etapa) e em [src/components/PortfolioGanttView.tsx](src/components/PortfolioGanttView.tsx) (mesmo gap encontrado durante a varredura).
- [x] Restringir perfil "padrão" para acesso **somente leitura**. Feito em duas camadas:
  - [src/utils/permissions.ts](src/utils/permissions.ts): perfil `padrao` só tem `view_project`.
  - Guard de rota em [src/App.tsx](src/App.tsx) para `#settings`/`#new-project` (bloqueia acesso direto via hash ou downgrade de perfil).
  - Enforcement de UI (não previsto no texto original do item, mas necessário para o "somente leitura" ser real): [GlpiAssetView.tsx](src/components/GlpiAssetView.tsx), [ActionPlanView.tsx](src/components/ActionPlanView.tsx), [EstimationScheduleView.tsx](src/components/EstimationScheduleView.tsx), [ProjectFollowUpView.tsx](src/components/ProjectFollowUpView.tsx) e os modais de anotação/impedimento/agendamento/prioridade/etapa em [ProjectPortfolioDashboard.tsx](src/components/ProjectPortfolioDashboard.tsx) agora recebem `userRole` e desabilitam campos/botões via `can(...)`.
- [x] Renomear "Diário de Bordo" para "Acompanhamento" em todo o projeto. `ArtifactsDiaryView` foi renomeado para `ProjectFollowUpView` (arquivo e componente); todos os textos de UI, comentários e a referência em [src/App.tsx](src/App.tsx) foram atualizados, incluindo ocorrências extras encontradas em [SettingsView.tsx](src/components/SettingsView.tsx).
